@if (!Settings.UseWebpackDevServer)
{
    @section styles {
        @Styles.Render("~/styles/home")
    }
}

@section scripts {
    @if (Settings.UseWebpackDevServer)
    {
        <script src="@(Settings.WebpackDevServerRoot)/home.js"></script>
    }
    else
    {
        @Scripts.Render("~/scripts/home")
    }
}
